import React from 'react';
import './App.css';

const Header = props => (
  <header className="App-header">
    <ul className="container">
      <li>Currency Exchange Project</li>
    </ul>
  </header>
);

export default Header;
